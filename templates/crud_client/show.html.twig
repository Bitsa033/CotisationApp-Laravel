{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <div class="container">

        <h1>Client n°{{ client.id }}</h1>
        
        <table class="table">
            <tbody>
                <tr>
                    <th>Nom</th>
                    <td>{{ client.nom }}</td>
                </tr>
                <tr>
                    <th>Contact</th>
                    <td>{{ client.contact }}</td>
                </tr>
            </tbody>
        </table>
        <div class="row">
            <div class="col-md-12">
                <a href="{{ path('crud_client_index') }}" class="btn btn-primary">back to list</a>
    
                <a href="{{ path('crud_client_edit', {'id': client.id}) }}" class="btn btn-info">edit</a>
            
                {{ include('crud_client/_delete_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
