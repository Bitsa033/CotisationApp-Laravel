{{ form_start(form) }}
{{ form_widget(form) }}
<div class="row">
    <div class="col-md-4">
        <a href="{{ path('crud_client_index') }}" class="btn btn-info offset-5">back to list</a>
        <button class="btn btn-success col-md-3">{{ button_label|default('Save') }}</button>
    </div>
</div>
{{ form_end(form) }}